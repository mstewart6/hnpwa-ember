<nav role="navigation">
  <ul class="navbar-links">
    <li class="navbar-link">
      {{#link-to 'top'}}
        <img src="{{rootURL}}/assets/images/y18.gif" class="brand" alt="logo">
      {{/link-to}}
    </li>

    {{#each links as |link|}}
      <li class="navbar-link {{if (eq active link) 'active'}}">
        {{#link-to link}}
          {{link}}
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</nav>
